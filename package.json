{
  "name": "oracle-layers-serverless",
  "version": "1.0.0",
  "description": "Sample node oracledb lambda layers example with serverless framework",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Colton Dick",
  "devDependencies": {
    "aws-sdk": "^2.944.0"
  },
  "dependencies": {
    "oracle-layers-serverless": "file:oraclelib/nodejs",
    "oracledb": "^5.3.0"
  }
}
